This directory contains the **utf-8 validation** project. The task is to build a validator function
that determines if an input data, represented as a list of integer values, has a valid utf-8
encoding.